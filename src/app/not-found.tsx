import { NotFoundState } from "@/components/ui/not-found-state";

export default function NotFound() {
  return <NotFoundState body="This page doesn't exist, or it moved. Let's get you back on the road." />;
}
